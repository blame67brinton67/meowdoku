# MeowDoku 本機遊戲站

## 啟動

```bash
npm install
npm start
```

開啟 `http://localhost:3000`。要讓朋友加入，可啟動 ngrok：

```bash
ngrok http 3000
```

## 管理員

右上角的「管理關卡」可產生並發布有唯一解的關卡。預設管理密鑰是
`meowdoku-admin`；正式使用前請以環境變數 `ADMIN_KEY` 更換。

```bash
$env:ADMIN_KEY = "請換成自己的密鑰" # PowerShell
npm start
```

### 用產題器建立單人關卡

1. 在首頁右上角按「管理關卡」。
2. 輸入管理密鑰、關卡名稱，並選擇尺寸。建議從 **7 × 7** 開始；10 × 10
   適合較長的挑戰。
3. 按「產生並發布唯一解關卡」。網站會先產生有連通色塊的地圖，再逐一驗證：
   每色、每行、每列各一隻貓，所有貓彼此不在八方向相鄰格，且全題只有一組解。
4. 成功訊息出現後，關卡立即會出現在單人關卡列表中，並會保存在
   `data/levels.json`，即使重啟伺服器也不會消失。

若產題時間較長，請稍候；發佈前不需要自行檢查答案或色塊連通性。

關卡與單人排行榜會保存於 `data/`。多人房間僅保留在伺服器記憶體中，伺服器
重新啟動後會自然結束。
